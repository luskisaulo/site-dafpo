backend:
  name: git-gateway
  branch: main # ou master, dependendo do nome do seu ramo principal no GitHub

media_folder: "img" # Pasta onde as imagens dos posts serão salvas
public_folder: "/img" # Como o site vai acessar essas imagens

collections:
  - name: "posts" # Identificador da coleção
    label: "Posts (Fique por Dentro)" # Nome que aparecerá no painel
    folder: "_posts" # Pasta onde os arquivos dos posts serão salvos
    create: true # Permite criar novos posts
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Como o nome do arquivo será gerado
    fields: # Os campos que você preencherá para cada post
      - {label: "Título", name: "title", widget: "string"}
      - {label: "Data de Publicação", name: "date", widget: "datetime"}
      - {label: "Imagem de Destaque", name: "thumbnail", widget: "image"}
      - {label: "Resumo (para a página inicial)", name: "excerpt", widget: "text"}
      - {label: "Conteúdo Completo do Post", name: "body", widget: "markdown"}